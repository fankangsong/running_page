* [x] 成功创建热力图页面并接入前端路由（可通过导航访问）。

* [x] 页面布局包含标题（左上）、热力图主体和右上角的年份选择器。

* [x] 页面在桌面端与移动端均能良好显示（移动端热力图可横向滑动）。

* [x] 年份选择器包含至少近 10 年选项，下拉或弹层交互正常，默认加载当前年份。

* [x] 热力图组件正确消费 `{ date: 'YYYY-MM-DD', value: number }[]` 数据并按所选年份聚合渲染。

* [x] 热力图缺省数据正确渲染为灰色块，有数据色块符合现有色系。

* [x] 切换年份时，图表区域有平滑的 Loading 动画，页面不出现闪屏现象。

* [x] 鼠标悬停在日块上能通过 Tooltip 正确显示该日期的详细数据。

* [x] 点击日块能够触发预留的回调函数接口。

* [x] 整体 UI（色彩、字体、间距、圆角、交互等）符合现有站点的设计体系。

